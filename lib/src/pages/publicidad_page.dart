import 'package:flutter/material.dart';

class PublicidadPage extends StatelessWidget {
  //const PublicidadPage({Key key}) : super(key: key);

  static final routeName = 'publicidad';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Publicidad page....'),
    );
  }
}