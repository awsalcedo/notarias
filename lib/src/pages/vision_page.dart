import 'package:flutter/material.dart';

class VisionPage extends StatelessWidget {
  //const VisionPage({Key key}) : sup//er(key: key);

  static final routeName = 'vision';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Vision Page...'),
    );
  }
}