import 'package:flutter/material.dart';

class MisionPage extends StatelessWidget {
  //const MisionPage({Key key}) : super(key: key);

  static final routeName = 'mision';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mision Page...'),
    );
  }
}